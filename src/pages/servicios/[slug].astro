---
// src/pages/servicios/[slug].astro
import Layout from '../../layouts/Layout.astro';
import serviciosData from '../../data/servicios.json';

const { slug } = Astro.params;
const servicio = serviciosData.serviciosEmpresa.find(s => s.slug === slug);

if (!servicio) {
    return Astro.redirect('/empresas');
}
---

<Layout title={servicio.nombre}>
    
    <div class="service-hero position-relative d-flex align-items-center">
        
        <div class="container position-relative z-2 py-5">
            <div class="row">
                <div class="col-lg-9 col-xl-8">
                    
                    <div class="bg-white p-4 p-lg-5 rounded-4 shadow-lg animate-up">
                        
                        <nav aria-label="breadcrumb" class="mb-3">
                            <ol class="breadcrumb mb-0">
                                <li class="breadcrumb-item">
                                    <a href="/" class="text-primary text-decoration-none small fw-bold">Inicio</a>
                                </li>
                                <li class="breadcrumb-item">
                                    <a href="/empresas" class="text-primary text-decoration-none small fw-bold">Servicios</a>
                                </li>
                                <li class="breadcrumb-item active text-primary small fw-bold" aria-current="page">
                                    {servicio.nombre}
                                </li>
                            </ol>
                        </nav>

                        <h1 class="display-4 fw-bold text-primary mb-3">{servicio.nombre}</h1>
                        
                        <p class="lead text-dark mb-0">
                            Soluciones integrales diseñadas para potenciar el rendimiento de tu organización.
                        </p>
                    </div>

                </div>
            </div>
        </div>
    </div>

    <div class="container py-5">
        <div class="row gx-5">
            <div class="col-lg-8 mb-5 mb-lg-0">
                <h2 class="h3 fw-bold text-primary mb-4 border-bottom pb-2">Descripción del Servicio</h2>
                <p class="fs-5 text-dark mb-5 leading-relaxed">
                    {servicio.descripcion} 
                    <br><br>
                    En Apex, entendemos que cada empresa es única. Por eso, adaptamos este servicio para cumplir con tus objetivos estratégicos, garantizando calidad, cumplimiento normativo y resultados medibles.
                </p>

                <h3 class="h4 fw-bold text-primary mb-4">¿Qué incluye este servicio?</h3>
                <div class="bg-light p-4 rounded-4 border">
                    <div class="row g-3">
                        {servicio.subServicios ? servicio.subServicios.map(item => (
                            <div class="col-md-6">
                                <div class="d-flex align-items-start">
                                    <i class="bi bi-check-circle-fill text-success me-2 mt-1"></i>
                                    <span class="fw-medium text-dark">{item}</span>
                                </div>
                            </div>
                        )) : (
                            <p class="text-muted">Contáctanos para conocer el alcance detallado.</p>
                        )}
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
    <div class="sticky-top" style="top: 2rem; z-index: 1;">
        <div class="card border-0 shadow rounded-4 overflow-hidden">
            <div class="card-header bg-primary p-4 text-center">
                <h4 class="mb-0 fw-bold text-white h5">¿Te interesa?</h4>
            </div>
            <div class="card-body p-4 text-center bg-white">
                
                <p class="text-dark small mb-4">
                    Solicita una cotización personalizada para <strong>{servicio.nombre}</strong>.
                </p>
                
                <a href="/contacto" class="btn btn-warning w-100 fw-bold mb-3">
                    Cotizar Ahora
                </a>
                <div class="small text-muted">
                    <i class="bi bi-telephone-fill me-1"></i> +51 991 057 926
                </div>
            </div>
        </div>
    </div>
</div>
</Layout>

<style define:vars={{ bgImage: `url("${servicio.imagen}")` }}>
    /* Estilos (Igual que antes) */
    .service-hero {
        min-height: 500px; 
        background-image: var(--bgImage); 
        background-size: cover;
        background-position: center;
        box-shadow: inset 0 0 0 2000px rgba(0, 0, 0, 0.2); 
    }

    .leading-relaxed {
        line-height: 1.8;
    }

    .animate-up {
        animation: fadeInUp 0.8s ease-out;
    }

    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
    
    .card-header h4 {
        color: #ffffff !important;
    }
</style>